'use client';

// Phase 1: /today re-renders the existing /priorities page. Phase 2
// will introduce a purpose-built Today page that composes priorities +
// plans + habits for the selected date with a List/Matrix view toggle.

import PrioritiesPage from '../priorities/page';

export default PrioritiesPage;
